spring:
  liquibase:
    change-log: classpath:/db/changelog/changes/db.changelog-master.xml
  datasource:
    url: jdbc:postgresql://localhost:5425/cafe
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
